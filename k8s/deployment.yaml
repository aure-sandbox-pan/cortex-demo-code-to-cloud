apiVersion: apps/v1
kind: Deployment
metadata:
  name: cortex-demo-app
  labels:
    app: cortex-demo-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cortex-demo-app
  template:
    metadata:
      labels:
        app: cortex-demo-app
    spec:
      containers:
        - name: cortex-demo-app
          # Replaced by .github/workflows/cd.yml with the real ECR image URI:tag at deploy time
          image: IMAGE_PLACEHOLDER
          ports:
            - containerPort: 5000
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
          livenessProbe:
            httpGet:
              path: /health
              port: 5000
          readinessProbe:
            httpGet:
              path: /health
              port: 5000
